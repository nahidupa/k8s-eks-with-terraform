apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blue-web-echo.fullname" . }}-test-connection"
  labels:
    {{- include "blue-web-echo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blue-web-echo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
