apiVersion: v1
kind: Service
metadata:
  name: {{ include "blue-web-echo.fullname" . }}
  labels:
    {{- include "blue-web-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blue-web-echo.selectorLabels" . | nindent 4 }}
