apiVersion: v1
kind: Service
metadata:
  name: {{ include "green-web-echo.fullname" . }}
  labels:
    {{- include "green-web-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "green-web-echo.selectorLabels" . | nindent 4 }}
